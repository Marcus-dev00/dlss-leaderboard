# 人数登记排行榜 — 项目规划（Supabase 方案）

## 1. 需求概述

- 使用场景：小公司内部工具，员工人数约 100 人
- 核心功能：员工登录后提交人数，系统按员工累计提交数排名，排行榜实时更新
- 登录方式：姓名 + 密码，简单登录，无需邮箱验证
- 提交规则：允许重复提交，每次提交的数字累加到该员工的总数上
- 排名规则：按累计总数从高到低排序

## 2. 技术方案

| 层 | 选型 | 说明 |
|---|---|---|
| 前端 | React | 网页应用，负责登录、提交、排行榜展示 |
| 数据库 | Supabase（Postgres） | 免费额度足够 100 人规模使用 |
| 鉴权 | Supabase Auth | 密码走标准加密存储，不用自己处理密码安全 |
| 实时更新 | Supabase Realtime | 基于 Postgres 变更订阅，数据一改所有在线页面自动收到更新，不用轮询 |
| 部署 | Vercel / Netlify（前端）+ Supabase（后端） | 免费额度内可覆盖 100 人规模的使用量 |

### 2.1 为什么选 Supabase

- 不用自己搭后端服务器，省去运维成本
- 内置账号系统，密码有标准加密，比自己存明文密码安全
- Realtime 订阅开箱即用，前端订阅一张表，数据变化立刻推送，不用写 WebSocket
- 免费额度：数据库 500MB、每月 5 万次 Realtime 消息、5 万月活用户，100 人规模的内部工具完全够用，可以先不考虑付费
- 后续如果要加导出、统计、多团队分组等功能，直接写 SQL 查询即可，不需要额外基建

### 2.2 登录方式的实现说明

Supabase Auth 默认围绕邮箱设计，但可以用「姓名 + 密码」的方式包一层：
- 员工输入姓名时，前端在后台把姓名转换成一个内部邮箱格式（例如 `wangxiaoming@internal.local`），对用户不可见
- 实际登录调用的还是 Supabase 标准的邮箱+密码登录接口，只是员工感知不到"邮箱"这一层
- 首次输入的姓名+密码 = 自动注册

## 3. 数据结构设计

### 3.1 用户表 `profiles`

Supabase Auth 自带 `auth.users` 表（存邮箱、加密密码），另建一张 `profiles` 表存业务信息，用 `id` 关联：

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键，关联 auth.users.id |
| name | text | 员工姓名，唯一，用户实际登录用的标识 |
| created_at | timestamp | 注册时间 |

### 3.2 提交记录表 `submissions`

| 字段 | 类型 | 说明 |
|---|---|---|
| id | uuid | 主键 |
| user_id | uuid | 关联 profiles.id |
| amount | integer | 本次提交的人数 |
| created_at | timestamp | 提交时间 |

> 保留每条提交记录而不是只存累计总数，方便追溯历史、按天/周统计、发现异常提交，后续做防刷限制也更容易。

### 3.3 排行榜查询

```sql
SELECT profiles.name, COALESCE(SUM(submissions.amount), 0) AS total
FROM profiles
LEFT JOIN submissions ON submissions.user_id = profiles.id
GROUP BY profiles.name
ORDER BY total DESC;
```

前端订阅 `submissions` 表的 `INSERT` 事件，一有新提交就重新拉一次排行榜（100 人规模数据量小，直接重新查询即可，不需要额外做增量计算）。

### 3.4 权限设置（Row Level Security）

Supabase 默认开启 RLS，需要配置策略：
- `profiles`：所有登录用户可读；只能修改自己的记录
- `submissions`：所有登录用户可读（排行榜要看到所有人）；只能以自己的 `user_id` 写入，不能替别人提交

## 4. 功能清单

### 4.1 核心功能

- [ ] 姓名 + 密码登录，首次输入自动注册
- [ ] 提交数字，累加到个人总数
- [ ] 排行榜按总数排序，前三名高亮
- [ ] 显示"我的排名"和"我的累计数"
- [ ] Realtime 自动刷新，不用手动刷新页面

### 4.2 待讨论 / 可选功能

- [ ] 管理员后台：清空数据、编辑/删除某条记录、重置密码
- [ ] 提交历史查看：每个人能看到自己每次提交的时间和数量
- [ ] 防刷机制：限制单次提交上限、限制提交频率
- [ ] 数据导出：导出排行榜为 Excel/CSV
- [ ] 分类目排名：如果有多个团队/部门，是否需要分组排名
- [ ] 通知：某人反超排名第一时是否需要提示

## 5. 待确认的问题

1. **密码找回**：员工忘记密码怎么办？是否需要管理员手动重置？
2. **数据修正**：填错数字了怎么撤回或修改？
3. **排行榜是否需要历史存档**：比如每月清零重新排名，还是永久累计？
4. **是否需要区分角色**：普通员工 vs 管理员，权限是否不同？
5. **Supabase 账号**：是否已有 Supabase 账号，还是需要从注册开始？

## 6. 推进步骤

1. 注册 / 确认 Supabase 账号，创建新项目
2. 建表：`profiles`、`submissions`，配置 RLS 权限策略
3. 前端接入 Supabase SDK，实现登录（姓名转内部邮箱的封装）
4. 实现提交表单、排行榜展示、Realtime 订阅
5. 本地联调测试，确认多用户同时使用时排行榜同步正常
6. 部署到 Vercel/Netlify，替换掉之前的 Demo 版
7. 视使用情况迭代第 4.2 节的可选功能
